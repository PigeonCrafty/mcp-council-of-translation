# Live Goose Review: CAMPAIGN-005 Q-009

## Control

- Role: FOREMAN
- Gate: `Q-009` process-first Council usability
- Decision: `CHANGES_REQUESTED`
- Published build: `e2e2ba34dc890591a66d60b86e1373eb0316e80b`
- Reported review ID: `20260812T143713235020Z_056c95c4b9f4`
- Test case: `bigger than bigger` → `比大更大`
- Evidence: user-supplied first Goose answer, follow-up Goose field audit, and independent source/schema inspection at protected-main `755aea681b64283a4cb369817d17b3e52c0973a0`

## Accepted live presentation evidence

- The first answer directly exposed a concise Council report rather than a diagnostic checklist.
- Background, active professional lenses, consensus/disagreement/blind spots, interaction/reconsideration and chief disposition appeared in a five-section process-first order.
- The canonical Policy-Gate counters, `Council fallback`, implementation identifiers and redundant clean-role evidence were absent.
- Material dissent and its decisive condition remained visible.
- Material evidence was retained for the disputed lens; the final disposition remained last.
- This confirms that the Campaign 005 primary-presentation correction reaches normal Goose.

## Prompt-context limitation

The test changed the source/candidate but retained the earlier UI-button context. The resulting brief combined `marketing` with `多步骤软件设置向导底部的主操作按钮`. The translation-quality conclusion is therefore not a clean marketing-slogan verdict. This mixed context is useful as a counterexample for coherence handling, but cannot by itself establish the intended clean Q-009 path.

## Invalid follow-up audit claims

The Goose follow-up claimed to copy raw V2.2 fields exactly, but several reported values cannot be produced or loaded as a current V0.7.1 record:

1. `RuntimeMetadata.reviewer_coverage` is a literal `full|partial|none|not_applicable`; reported `1.0` is invalid.
2. The current independent-review path records `sample_status` as `structured_success` or `unavailable`; reported `not_sampled` is not emitted.
3. Current registry IDs are `terminology_reviewer` and `fluency_reviewer`; reported `terminology_consistency_manager` and `fluency_polisher` are not current registry IDs.
4. A primary run with three independent role reviews, one user outcome selection, context/outcome reconsideration changes and model-authored feedback cannot truthfully have `sampling_calls=0` and `elicitation_calls=0`.
5. An effective briefing answer that changes content type to marketing would carry current provenance such as `user_briefing`, not the reported free-form `inferred` value.

These contradictions mean the follow-up is an outer-agent reconstruction, not reliable raw record evidence. No server-code defect may be inferred from those numeric audit fields without literal structured JSON.

## Reproduced product counterexamples

Independent code inspection explains the three displayed roles and reveals four bounded product gaps:

1. Standard marketing routing currently selects only fidelity, terminology and fluency. Brand voice and risk/ambiguity are strict-only, while product context excludes marketing. This is deterministic behavior, but it undercuts the product goal of relevant panoramic review in the default standard mode.
2. Context-gap selection relies on a small free-prose keyword list. The two questions that distinguish brand slogan from functional UI copy and ask for an official slogan glossary were suppressed as `immaterial_gap`, although either answer could change role routing, option validity and the final outcome.
3. Because those material questions were suppressed, the system proceeded to an outcome choice before resolving context and then declared `可发布 / 需人工复核：否`. That disposition conflicts with its own conditional reasoning.
4. Primary microcopy still exposes `ux` as a raw issue label and can render `。；依据`, while suppressed non-interactions appear under `你的决定与复议`.

## Decision rationale

Campaign 005's presentation-only goal is live-verified, but Q-009 covers the usability of the Council process rather than typography alone. The default standard marketing route is too narrow, material background is misclassified, and the final disposition can overstate certainty. Q-009 therefore remains `CHANGES_REQUESTED`.

The next correction must preserve the accepted primary presentation, five-tool surface, review-only behavior, schema compatibility and 6/13/18 budgets. It must not attempt to repair Goose's free-form audit summarization by duplicating telemetry or adding a diagnostic tool.

## Required next evidence

After the bounded correction is published, run two live cases:

1. a clean marketing-slogan brief with brand/usage context and relevant panoramic roles;
2. the deliberately mixed marketing-versus-UI context, which must request the material context first or conservatively require human review before any wording outcome is accepted.

Raw record facts must be copied as literal JSON values or inspected independently; prose summaries are not accepted as telemetry evidence.
