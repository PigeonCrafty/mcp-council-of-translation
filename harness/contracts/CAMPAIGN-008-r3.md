# Campaign Contract: CAMPAIGN-008-r3

## Control

- HARNESS_ROLE: WORKER
- HARNESS_MODE: STRICT_CAMPAIGN
- State: ASSIGNED
- Repository: `C:\Users\GeZhu\MyMCP\mcp-council-of-translation`
- Exact Git baseline: `6464f96f681aa3531c14cd631689673561193027`
- Baseline subject: `Execute V2.4 offline golden corpus`
- Supersedes for execution: `CAMPAIGN-008-r2`
- Product identifiers remain: package/module `0.10.0`, build
  `evidence-value-council-v8`, schema `2.4`
- Required Worker report: `harness/reports/CAMPAIGN-008-r3-worker.md`
- New ledger: not required; preserve the r1 ledger unchanged
- Commit policy: exactly one scoped local commit
- Subagent delegation: forbidden
- Acceptance authority: Foreman only

Read completely before editing: `AGENTS.md`, `harness/plan.md`,
`harness/features.json`, `harness/progress.md`, this contract,
`harness/contracts/CAMPAIGN-008-r1.md`,
`harness/contracts/CAMPAIGN-008-r2.md`,
`harness/evaluations/CAMPAIGN-008-r1-review.md`,
`harness/evaluations/CAMPAIGN-008-r2-review.md`, and both r1/r2 Worker reports plus the
r1 ledger.

## Bounded correction outcome

Complete PKG-047 by correlating equivalent deterministic preflight diagnostics and
matching reviewer findings across every existing machine-enforced check family. Preserve
the original clusters/evidence and change only the descriptive logical-issue counts and
role contribution projection. The accepted r2 executable Golden runner, evaluator,
digest and all public behavior are frozen.

## Accepted evidence to preserve

- All r1 V2.4 models, compatibility, persistence, value-first display and V0.10 package
  evidence.
- r2 placeholder/markup correlation, unavailable precedence and removal of prose-based
  contribution classification.
- r2 executable 18-case corpus, real context/outcome/continuation authority paths,
  JSON-safe aggregate and negative input/envelope mutations.
- Exact five tools, review-only, roles/prompts/Policy Gate/user authority, schema 2.4,
  budgets 6/13/18, concurrency controls and package/build identifiers.

## Required correction

1. Create bounded exact logical aliases rooted in deterministic preflight structure, not
   natural-language topic/problem/evidence. Cover all existing families:
   braced/printf placeholders, variables, command tokens, tags, URLs, explicit DNT,
   `required_literal`, `forbidden_literal`, numeric parity and Markdown structure.
2. Correlate a reviewer cluster with a deterministic cluster only when an exact normalized
   source/candidate anchor or exact caller-constraint literal matches a deterministic
   alias. Do not use substring semantic scoring, n-gram/fuzzy similarity, embeddings,
   translated words, free-form evidence text or a model call.
3. Correlate overlapping deterministic scanners when they are exact views of the same
   token. In particular, a missing `https://example.com` currently produces
   command-parity `/example` and URL-parity `https://example.com`; retain both clusters
   but count one logical material issue.
4. Strip only recognized deterministic prefixes such as `required_literal:` and
   `forbidden_literal:` when deriving an exact alias. Never interpret arbitrary caller
   prose as a constraint.
5. Preserve every `IssueCluster`, finding, preflight check and evidence entry byte-for-
   structure in the record. Only `CouncilValueMetrics` grouping/counts may change.
6. Preserve `unavailable` as the highest contribution kind while retaining deterministic
   issue counts on that role.
7. Prove non-overmerging: a placeholder loss plus a distinct missing URL remains two
   logical issues; two distinct caller literals remain two; common role/category alone
   never joins issues.

## Required counterexamples

Add direct focused tests for at least:

- `required_literal:Acme` plus a matching technical finding for `Acme` -> one issue;
- `forbidden_literal:危险` plus matching reviewer evidence -> one issue;
- hard `numeric_parity` 10 -> 9 plus matching finding for `10` -> one issue;
- hard Markdown heading/list/link/fence signal plus exact matching structured finding ->
  one issue;
- explicit DNT loss plus matching finding -> one issue;
- missing URL preflight-only command/url overlap -> one issue while both clusters remain;
- placeholder and URL together -> two issues;
- unavailable technical sample on one of the above -> unavailable role, truthful issue
  count and no false-clean primary statement.

Existing placeholder, tag, duplicate-same-role, corroboration, discussion and clean
counterexamples must remain green.

## Authorized paths

- `src/council_of_translation/localization/value_metrics.py`
- `tests/unit/test_v24_value_metrics.py`
- `tests/integration/test_v24_value_metrics.py`
- required `harness/reports/CAMPAIGN-008-r3-worker.md`

Every other production, test, fixture, evaluation, package, documentation, dependency,
lock, Harness and user path is forbidden. Do not stage or commit the Worker report or any
protected asset.

## Admission and protected assets

Verify exact baseline, empty index, contract hash, declared dirty/untracked set and every
hash below before editing. Admission compile and full suite must pass with exactly or at
least the accepted `269 passed`; stop on unexplained drift.

| Protected asset | SHA-256 |
| --- | --- |
| `harness/plan.md` | `98EB6C387FEE226615F1C342CB5BC7288E020027523235F91542BFAAA758800D` |
| `harness/features.json` | `A5E1A5030C9A307F4A3FE55682D9E5F49A6789C11D34C20F5A407084141DF984` |
| `harness/progress.md` | `F90D9BD2629FA6E8AD1F69493AE767876E1B86E316329337F3835B2A836DA3C0` |
| `harness/contracts/CAMPAIGN-008-r1.md` | `3D477E8418B77A621EFEBB3BD496CD0508BCC9C39A22D833EFA79886A98EE366` |
| `harness/contracts/CAMPAIGN-008-r2.md` | `9F01492711FDCA0CCF27D74851E8A3FDB26DA6454524CC4DAA799FA48E1201BB` |
| `harness/evaluations/CAMPAIGN-008-r1-review.md` | `D85B7C35026C394001C7C17DE5FCE591128D917BB1961FA67ADED19E88FE3292` |
| `harness/evaluations/CAMPAIGN-008-r2-review.md` | `AC8E122CFC3AF539E4E74E8B8DE99845258D81F7AFF7B28BF87EB1F8850DE6EC` |
| `harness/reports/CAMPAIGN-008-r1-worker.md` | `412A1E032B919289630EAE58A386B45EF5869B10C91C9FEC76C78313DC8AA37F` |
| `harness/reports/CAMPAIGN-008-r1-ledger.md` | `26AD64BE56B776B9EECD07F927C116E9B360746194D1CE026E3AEE0295A5068A` |
| `harness/reports/CAMPAIGN-008-r2-worker.md` | `12E9B7ECDE549ACCCF79B649446D7838BBDA586589363B4E86A2E92A9177A698` |
| `.learnings/LEARNINGS.md` | `52DC1BA8043481911C0DEABB3AC882504D9CBE8BB63D60F391C188586A230DF0` |
| `.learnings/ERRORS.md` | `F99EB75886781958BF5AE6DCF844FD476CBCA45937896A25EE3B2E9D6CD34F0A` |
| audit Markdown | `B48073E063C4C9F34E0B6B4DAE94E80079EAAAD3B2F8E273DD60B2F25F6ABD76` |

All other contracts/evaluations plus `reviews/**` and `myTest/**` are path-protected.
The admitted dirty/untracked assets are intentional; do not restore, stage, commit,
rename or delete them.

## Required verification

1. Admission compile and complete `269`-test baseline regression.
2. Run every new exact-correlation and non-overmerge counterexample.
3. Run all V2.4 metrics, presentation and Golden suites; the unchanged executable corpus
   must remain exactly 18/18 with no fixture-authored observations.
4. Run compatibility/persistence/privacy, full tool/version/budget/concurrency invariants
   and prove value computation adds zero sampling/elicitation.
5. Final `python -m compileall src tests` and complete regression.
6. `git diff --check 6464f96f681aa3531c14cd631689673561193027..HEAD`, exact
   three-path implementation/test scope, protected hashes, dead-import scan and empty
   index.
7. Build fresh wheel/sdist and run isolated Python 3.12/current-FastMCP smoke through all
   five tools plus the required-literal, numeric and URL counterexamples.

## Stop conditions and handoff

Stop `BLOCKED` only if this requires changing preflight checks, clustering, schema,
public behavior, dependencies, another unauthorized path, or live/external authority.
A failing newly added test is normal Worker implementation work, not a blocker.

Write `harness/reports/CAMPAIGN-008-r3-worker.md`. Start the handoff with exactly
`READY_FOR_REVIEW` or `BLOCKED`, then report contract hash, baseline/final HEAD, the one
commit and exact paths, every named counterexample, focused/Golden/full/build/wheel
results, preserved evidence, hashes/index/worktree, subagent/authority/external/live
counts and remaining risks. Do not push, create/update a PR, release, deploy, call
Goose/provider/model, claim Campaign acceptance, Q-012 acceptance or project completion.

