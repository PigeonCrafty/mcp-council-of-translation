# Campaign Contract: CAMPAIGN-008-r2

## Control

- HARNESS_ROLE: WORKER
- HARNESS_MODE: STRICT_CAMPAIGN
- State: ASSIGNED
- Repository: `C:\Users\GeZhu\MyMCP\mcp-council-of-translation`
- Exact Git baseline: `6e28c103f98a6b0481ab7d103580b83f8e6c4cfa`
- Baseline subject: `Release V0.10 evidence value contract`
- Supersedes for execution: `CAMPAIGN-008-r1`
- Product identifiers remain: package/module `0.10.0`, build
  `evidence-value-council-v8`, schema `2.4`
- Required Worker report: `harness/reports/CAMPAIGN-008-r2-worker.md`
- New ledger: not required; preserve the r1 ledger unchanged
- Commit policy: exactly two scoped local commits, one per correction package
- Subagent delegation: forbidden in the shared worktree
- Acceptance authority: Foreman only

Read completely before editing: `AGENTS.md`, `harness/plan.md`,
`harness/features.json`, `harness/progress.md`, this contract,
`harness/contracts/CAMPAIGN-008-r1.md`,
`harness/evaluations/CAMPAIGN-008-r1-review.md`,
`harness/reports/CAMPAIGN-008-r1-worker.md`, and
`harness/reports/CAMPAIGN-008-r1-ledger.md`.

## Bounded correction outcome

Make V2.4 value claims truthful for deterministic preflight blockers and turn the
18-case Golden Corpus into executable offline product evidence. Preserve all accepted r1
models, persistence, ordinary contribution/discussion behavior, presentation structure,
public behavior and V0.10 packaging.

## Accepted r1 evidence to preserve

- V2.4 bounded models, serialization, privacy-safe metadata and V1/V2.0-V2.3 reads.
- Ordinary validated model-finding contribution classes, per-role deduplication,
  unavailable precedence and discussion trace-delta semantics.
- Zero extra sampling/elicitation for value metrics and presentation.
- Five-section value-first report, active-role accounting, minority/degradation
  visibility, chief-last behavior and 3,200-code-point hard bound.
- Exact five tools, review-only behavior, user authority, Policy Gate, role routing,
  budgets 6/13/18, concurrency controls and all V0.10/schema 2.4 identifiers.

## PKG-047 — structured preflight contribution integrity

1. Deterministic blocking/warning preflight clusters with registered participant roles
   are structured material evidence even when they have no model `finding_ids`.
2. Attribute a preflight-only placeholder/markup integrity issue to the relevant active
   technical role; do not report that role as confirmation-only and do not claim that no
   material issue exists.
3. Correlate equivalent deterministic and model-supported findings by bounded structured
   issue identity. One underlying issue contributes once to aggregate unique/
   corroborated counts even if both preflight and the technical reviewer report it.
   Preserve both evidence sources in the full record.
4. Repeated/synonymous findings from one role remain deduplicated. Do not introduce
   majority authority, semantic prose matching, fuzzy embedding logic or a model call.
5. `unavailable` remains the highest role-classification priority. A technical role that
   failed sampling may not be relabeled as a successful unique contributor merely because
   deterministic preflight identified an issue; the aggregate issue must still remain
   visible and the unavailable coverage must remain truthful.
6. Remove free-form role-lens/substr-based semantic classification from
   `_fallback_value_metrics`. A missing-metrics compatibility display may conservatively
   account for roles and unavailable structured status, but it must not infer unique or
   corroborating value from natural prose.
7. Add direct and full-orchestration counterexamples for placeholder loss, broken markup,
   preflight plus equivalent technical finding, unavailable technical sample and clean
   preflight. Primary text must never combine “未发现新增实质问题” with a later visible
   deterministic blocker.

## PKG-048 — executable 18-case Golden Corpus

1. Keep exactly the 18 frozen audit scenarios and declarative machine-checkable expected
   properties. Remove fixture-authored `observed` values; duplicated expected/observed
   dictionaries are forbidden.
2. Provide a deterministic offline runner/library path that constructs each scenario,
   invokes real production components and derives its observed properties from the
   resulting `ReviewRecordV2`/continuation record. At minimum, the executed path must
   exercise the relevant preflight, validated reviewer envelopes, clustering, context or
   outcome interaction when applicable, Policy Gate, adjudication, value metrics and chief
   consistency. Use `run_structured_review`/`continue_structured_review` where practical.
3. Scripted model and user-interaction gateways are allowed and expected. Their envelopes
   are deterministic test inputs; they may not precompute the expected final aggregate.
   No network, provider, Goose or hidden model call is allowed.
4. Cases 16 and 17 must actually exercise user context/preference authority through the
   product interaction/continuation path. A valid preference may change a nonblocked
   decision; it may not override a deterministic blocker.
5. Derive JSON-safe aggregate metrics from the executed observations. Critical recall,
   false positives, contribution behavior, conflict detection, chief consistency,
   user-authority boundary, call budget and discussion marginal value must all be
   machine-checked.
6. Regression counterexamples must mutate scenario inputs or scripted envelopes and then
   rerun product behavior. Hand-editing a fixture `observed` dictionary is not acceptance
   evidence.
7. The exact corpus must catch the reproduced placeholder-loss defect and include at
   least one clean/no-conflict case that guards against invented contribution or conflict.

## Frozen invariants

- No new MCP tool, public parameter, runtime dependency, model call, retry, elicitation,
  discussion or reconsideration.
- Do not change prompts, role routing, user authority, Policy Gate, outcome identity,
  coverage rules, adjudication or full-history evidence.
- No version/schema/build bump beyond the accepted r1 `0.10.0` / `2.4` /
  `evidence-value-council-v8`.
- `uv.lock` and package metadata are frozen in r2.
- No free-form substring heuristic, hidden score, role ranking or voting weight.

## Authorized implementation paths

- `src/council_of_translation/localization/value_metrics.py`
- `src/council_of_translation/localization/digest.py`
- `src/council_of_translation/evaluation.py`
- `tests/fixtures/v24_golden_corpus.json`
- `tests/unit/test_v24_value_metrics.py`
- `tests/integration/test_v24_value_metrics.py`
- `tests/integration/test_v24_presentation.py`
- `tests/integration/test_v24_golden_corpus.py`
- required `harness/reports/CAMPAIGN-008-r2-worker.md`

No other production, test, fixture, package, documentation, dependency, Harness or user
path is authorized. Do not stage or commit any report or protected asset.

## Admission and protected assets

Before editing, verify exact baseline, empty index, declared dirty/untracked set,
contract hash and all protected assets. Admission compile and full suite must pass with at
least the r1 result of `263 passed`; stop on unexplained drift.

Protect unchanged and verify the exact hashes below at admission and handoff:

| Protected asset | SHA-256 |
| --- | --- |
| `harness/plan.md` | `D1782282422F656AAE4F9405988547E6ED220A72287F1CBA0ECF4DB1015492D1` |
| `harness/features.json` | `A5E1A5030C9A307F4A3FE55682D9E5F49A6789C11D34C20F5A407084141DF984` |
| `harness/progress.md` | `CBD623C7C0E45FA80164D51F737A2C3D5ACAFF8AE99E350D6CF5729E956892A3` |
| `harness/contracts/CAMPAIGN-008-r1.md` | `3D477E8418B77A621EFEBB3BD496CD0508BCC9C39A22D833EFA79886A98EE366` |
| `harness/evaluations/CAMPAIGN-008-r1-review.md` | `D85B7C35026C394001C7C17DE5FCE591128D917BB1961FA67ADED19E88FE3292` |
| `harness/reports/CAMPAIGN-008-r1-worker.md` | `412A1E032B919289630EAE58A386B45EF5869B10C91C9FEC76C78313DC8AA37F` |
| `harness/reports/CAMPAIGN-008-r1-ledger.md` | `26AD64BE56B776B9EECD07F927C116E9B360746194D1CE026E3AEE0295A5068A` |
| `.learnings/LEARNINGS.md` | `52DC1BA8043481911C0DEABB3AC882504D9CBE8BB63D60F391C188586A230DF0` |
| `.learnings/ERRORS.md` | `F99EB75886781958BF5AE6DCF844FD476CBCA45937896A25EE3B2E9D6CD34F0A` |
| audit Markdown | `B48073E063C4C9F34E0B6B4DAE94E80079EAAAD3B2F8E273DD60B2F25F6ABD76` |

All other contracts/evaluations plus `reviews/**` and `myTest/**` are path-protected even
when not enumerated in this table.

The admitted uncommitted Foreman/user assets are intentional. Do not restore, stage,
commit, rename or delete them.

## Required verification

1. Admission `python -m compileall src tests` and complete suite at exact baseline.
2. PKG-047 focused unit/integration counterexamples plus complete regression.
3. PKG-048 exact 18-case execution, aggregate metric assertions and negative
   counterexamples plus complete regression.
4. Explicit before/after evidence for the reproduced `Delete {count} files` -> `删除文件`
   case, including metric fields, role classification, chief status and primary report.
5. Explicit preflight-plus-model duplicate evidence showing one issue-local aggregate,
   plus unavailable-role evidence showing both material issue visibility and truthful
   unavailable coverage.
6. Prove all Golden observations are produced at runtime: fixture has no `observed`
   object and the runner calls real product orchestration/components.
7. Prove contribution/presentation/evaluation adds zero production sampling or
   elicitation beyond each scenario's scripted baseline and stays within 6/13/18.
8. V1/V2.0-V2.3 compatibility, V2.4 full/metadata persistence, presentation bounds,
   exact five tools, versions, budgets and concurrency invariants remain green.
9. Final compile, complete suite, `git diff --check` from the r2 baseline, exact-path
   scope audit, protected-asset hash audit, dead-import scan, empty index.
10. Build fresh wheel/sdist and run isolated Python 3.12/current-FastMCP smoke calling all
    five tools and one scripted review that exposes corrected preflight value metrics.

## Stop conditions and handoff

Stop `BLOCKED` only if the correction requires a public/schema/version/dependency change,
an unauthorized path, live/external behavior, or cannot preserve the accepted r1
evidence. A failing newly added regression is ordinary Worker work, not a stop condition.

Write `harness/reports/CAMPAIGN-008-r2-worker.md`. Start the handoff with exactly
`READY_FOR_REVIEW` or `BLOCKED`, then report contract hash, baseline/final HEAD, exactly
two commits and exact paths, before/after counterexamples, executable corpus evidence,
focused/full/build/wheel results, preserved r1 invariants, hashes/index/worktree,
subagent/authority/external/live counts and remaining risks. Do not push, open/update a
PR, release, deploy, call Goose/provider/model, claim Campaign acceptance, Q-012
acceptance or project completion.
